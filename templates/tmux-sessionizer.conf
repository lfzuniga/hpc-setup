# Per-HPC project paths. The fork sources this file as Bash.
# :1 means search one level below the listed directory.
# Add ONLY the particular parent directories containing your projects.
TS_SEARCH_PATHS=(
    "$HOME/.config:1"
)
